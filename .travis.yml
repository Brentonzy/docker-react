language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t brenton75/docker-react -f Dockerfile.dev .


script:
  - docker run -e CI=true brenton75/docker-react npm run test